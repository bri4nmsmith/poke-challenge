import React from "react"

export default function Footer(){
    return (
        <footer> Notes on changes:
            --fetch data from API <br />
            --display on screen<br />
            --input search - search onchange<br />
            --filter 1 onchange return results<br />
            --filter 2 onchange return results<br />
            -- multi-select filter reduces results.<br />
            -- combine filters into single method <br />
            -- refactor to use Hooks (useState and useEffect) <br /> 
            <hr/>
            
        </footer>
    )
}